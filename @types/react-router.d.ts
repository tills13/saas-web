// import React from "react"
// import { Route } from "react-router"
// import { Store, RootContainer } from "react-relay"
// import { } from "react."

/* declare module "react-router" {
  // interface Route extends RootContainer { }

  interface RouterProps {
    environment: Store
  }

  interface IndexRouteProps {
    queries?: { [query: string]: () => string }
    render?: (options: RelayRenderOptions) => React.ReactElement<any>
  }

  type RelayRenderOptions = { props: any, routerProps: any, element: React.ReactElement<any> }
} */